import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mon-budget-ionic-vue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
